# assets/

Not rendered as a site page (the project only renders `*.qmd`).

Add here:

- `Serebrennikov_CV.pdf` — the CV navbar link in `_quarto.yml` points to `assets/Serebrennikov_CV.pdf`. Until this file exists, that link 404s.
- `figures/` — already has `schema-network.svg` (schematic, used on the About and Publications pages). Real figures from published papers (near-repeat maps, CCTV placement plots) can go here too, referenced from `publications.qmd`.
